#!/bin/bash

# Install Tor Browser
echo "Installing Tor Browser..."
# Add commands here to install Tor Browser
# For example, on Ubuntu/Debian:
# sudo apt update
# sudo apt install torbrowser-launcher

# Install netcat (nc)
echo "Installing netcat (nc)..."
# Add commands here to install netcat
# For example, on Ubuntu/Debian:
# sudo apt update
# sudo apt install netcat

# Provide execution permissions to the main script
echo "Setting execution permissions for the main script..."
chmod +x main_script.sh

echo "Setup complete!"